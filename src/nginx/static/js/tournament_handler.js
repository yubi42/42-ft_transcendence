


export function joinLocalTournament(lobby, lobby_name, max_score, pac_pong)
{
    alert("no join tournament yet ");
}

export function joinTournament(lobby, lobby_name, max_score, pac_pong)
{
    alert("no join tournament yet ");
}